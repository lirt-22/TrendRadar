# TrendRadar 部署和使用指南

## 📋 项目简介

TrendRadar 是一个热点新闻聚合工具，可以：
- 聚合 11+ 个主流平台的热点新闻（知乎、微博、抖音、B站、百度等）
- 根据关键词智能筛选你关心的新闻
- 通过多种渠道推送通知（企业微信、飞书、钉钉、Telegram、邮件、ntfy）
- 支持 AI 智能分析（基于 MCP 协议）

---

## 🚀 部署方式选择

### 方式一：GitHub Actions 云端部署（推荐新手）

**优点**：无需服务器，免费，配置简单  
**缺点**：依赖 GitHub Actions 资源，执行时间不稳定

#### 部署步骤：

1. **Fork 项目**
   - 访问 https://github.com/sansan0/TrendRadar
   - 点击右上角 "Fork" 按钮

2. **配置通知渠道（GitHub Secrets）**
   - 进入你 Fork 的仓库
   - 点击 `Settings` → `Secrets and variables` → `Actions`
   - 点击 `New repository secret` 添加以下任一或多个：
   
   **企业微信**（最简单）：
   - Name: `WEWORK_WEBHOOK_URL`
   - Value: 你的企业微信机器人 Webhook 地址
   - 获取方式：企业微信 App → 群聊 → 右上角"…" → 消息推送 → 添加机器人 → 复制 Webhook

   **飞书**：
   - Name: `FEISHU_WEBHOOK_URL`
   - Value: 飞书机器人 Webhook 地址
   - 获取方式：https://botbuilder.feishu.cn/ 创建机器人指令

   **钉钉**：
   - Name: `DINGTALK_WEBHOOK_URL`
   - Value: 钉钉机器人 Webhook 地址
   - 获取方式：钉钉 PC 端 → 群设置 → 机器人 → 添加自定义机器人

   **Telegram**：
   - Name: `TELEGRAM_BOT_TOKEN` 和 `TELEGRAM_CHAT_ID`
   - 获取方式：搜索 @BotFather 创建机器人

   **邮件**：
   - Name: `EMAIL_FROM`, `EMAIL_PASSWORD`, `EMAIL_TO`
   - 支持 Gmail、QQ邮箱、163邮箱等

   **ntfy**：
   - Name: `NTFY_TOPIC`, `NTFY_SERVER_URL`（可选）

3. **配置关键词**
   - 编辑 `config/frequency_words.txt`
   - 添加你关心的关键词，每行一个
   - 支持语法：
     - 普通词：`华为`（包含即匹配）
     - 必须词：`+手机`（必须同时包含）
     - 过滤词：`!广告`（包含则排除）
   - 用空行分隔不同词组

4. **配置推送模式**
   - 编辑 `config/config.yaml`
   - 修改 `report.mode`：
     - `daily`：当日汇总（每小时推送所有匹配新闻）
     - `current`：当前榜单（每小时推送最新榜单）
     - `incremental`：增量监控（仅推送新增内容）

5. **调整推送频率**（可选）
   - 编辑 `.github/workflows/crawler.yml`
   - 修改 `cron` 表达式（建议不低于 30 分钟）

6. **手动测试**
   - 进入仓库的 `Actions` 页面
   - 找到 "Hot News Crawler" 工作流
   - 点击 "Run workflow" 手动执行一次
   - 等待 1 分钟左右查看是否收到推送

---

### 方式二：Docker 部署（推荐有服务器用户）

**优点**：完全自主控制，执行时间精准，可自定义  
**缺点**：需要服务器或本地 Docker 环境

#### 快速部署（一行命令）：

```bash
# 1. 创建配置目录
mkdir -p config output

# 2. 下载配置文件
wget https://raw.githubusercontent.com/sansan0/TrendRadar/master/config/config.yaml -P config/
wget https://raw.githubusercontent.com/sansan0/TrendRadar/master/config/frequency_words.txt -P config/

# 3. 运行容器
docker run -d --name trend-radar \
  -v ./config:/app/config:ro \
  -v ./output:/app/output \
  -e FEISHU_WEBHOOK_URL="你的飞书webhook" \
  -e CRON_SCHEDULE="*/30 * * * *" \
  -e RUN_MODE="cron" \
  -e IMMEDIATE_RUN="true" \
  wantcat/trendradar:latest
```

#### Docker Compose 部署（推荐）：

```bash
# 1. 创建项目目录
mkdir -p trendradar/{config,docker}
cd trendradar

# 2. 下载配置文件
wget https://raw.githubusercontent.com/sansan0/TrendRadar/master/config/config.yaml -P config/
wget https://raw.githubusercontent.com/sansan0/TrendRadar/master/config/frequency_words.txt -P config/
wget https://raw.githubusercontent.com/sansan0/TrendRadar/master/docker/docker-compose.yml -P docker/

# 3. 创建 .env 文件（在 docker 目录下）
cd docker
cat > .env << EOF
# 通知渠道
FEISHU_WEBHOOK_URL=你的飞书webhook
DINGTALK_WEBHOOK_URL=你的钉钉webhook
WEWORK_WEBHOOK_URL=你的企业微信webhook

# 运行配置
CRON_SCHEDULE=*/30 * * * *
RUN_MODE=cron
IMMEDIATE_RUN=true
EOF

# 4. 启动服务
docker-compose up -d

# 5. 查看日志
docker logs -f trend-radar
```

#### Docker 管理命令：

```bash
# 查看运行状态
docker exec -it trend-radar python manage.py status

# 手动执行一次爬虫
docker exec -it trend-radar python manage.py run

# 查看实时日志
docker exec -it trend-radar python manage.py logs

# 查看配置
docker exec -it trend-radar python manage.py config

# 重启容器
docker restart trend-radar

# 停止容器
docker stop trend-radar

# 更新镜像
docker pull wantcat/trendradar:latest
docker-compose down
docker-compose up -d
```

---

### 方式三：本地部署（主要用于 AI 分析功能）

**适用场景**：使用 MCP AI 分析功能，需要本地运行 MCP 服务器

#### Mac/Linux 部署：

```bash
# 1. 运行一键部署脚本
./setup-mac.sh

# 2. 启动 HTTP 服务（用于 MCP 客户端连接）
./start-http.sh
```

#### Windows 部署：

```bash
# 运行 Windows 部署脚本
setup-windows.bat

# 启动 HTTP 服务
start-http.bat
```

#### 配置 MCP 客户端：

**Cursor 配置**（项目级）：
在项目根目录创建 `.cursor/mcp.json`：

```json
{
  "mcpServers": {
    "trendradar": {
      "url": "http://localhost:3333/mcp",
      "description": "TrendRadar 新闻热点聚合分析"
    }
  }
}
```

**Claude Desktop 配置**：
编辑配置文件（Mac: `~/Library/Application Support/Claude/claude_desktop_config.json`）：

```json
{
  "mcpServers": {
    "trendradar": {
      "command": "uv",
      "args": [
        "--directory",
        "/path/to/TrendRadar",
        "run",
        "python",
        "-m",
        "mcp_server.server"
      ]
    }
  }
}
```

---

## ⚙️ 配置文件说明

### config/config.yaml

主要配置项：

```yaml
# 推送模式
report:
  mode: "daily"  # daily | current | incremental

# 通知设置
notification:
  enable_notification: true
  push_window:
    enabled: false  # 是否启用推送时间窗口
    time_range:
      start: "20:00"
      end: "22:00"
    once_per_day: true  # 每天只推送一次

# 监控平台（可自定义添加）
platforms:
  - id: "zhihu"
    name: "知乎"
  - id: "weibo"
    name: "微博"
  # ... 更多平台
```

### config/frequency_words.txt

关键词配置示例：

```txt
# 第1组：手机发布
华为
OPPO
+发布

# 第2组：股市行情
A股
上证
+涨跌
!预测

# 第3组：AI相关
AI
ChatGPT
人工智能
!广告
```

**语法说明**：
- 普通词：直接写关键词
- 必须词：`+关键词`（必须同时包含）
- 过滤词：`!关键词`（包含则排除）
- 空行分隔不同词组

---

## 📱 使用场景

### 1. 投资者监控
- 配置关键词：`A股`、`比特币`、`特斯拉` 等
- 模式选择：`incremental`（增量监控）
- 推送渠道：企业微信/Telegram

### 2. 自媒体人追踪热点
- 配置关键词：`AI`、`ChatGPT`、`热点事件` 等
- 模式选择：`current`（当前榜单）
- 推送渠道：飞书/钉钉

### 3. 企业舆情监控
- 配置关键词：公司名、产品名、品牌名
- 模式选择：`daily`（当日汇总）
- 推送渠道：企业微信/邮件

---

## 🤖 AI 智能分析功能

### 功能说明

基于 MCP 协议，支持自然语言查询和分析新闻数据：

- **基础查询**：查询最新新闻、按日期查询、热门话题
- **智能检索**：关键词搜索、相关新闻查找
- **高级分析**：趋势分析、数据洞察、情感分析、摘要生成

### 使用前提

- 项目至少运行一天（积累新闻数据）
- 本地部署 MCP 服务器（运行 `start-http.sh`）
- 配置 MCP 客户端（Cursor、Claude Desktop 等）

### 使用示例

```
查询今天知乎的热点新闻，前10条
分析"人工智能"这个话题最近一周的热度趋势
对比知乎和微博平台对"比特币"的关注度
生成今天的热点摘要报告
```

---

## 🔧 常见问题

### Q1: GitHub Actions 不执行？

- 检查工作流文件是否存在：`.github/workflows/crawler.yml`
- 确认仓库已启用 Actions（Settings → Actions → General）
- 手动触发一次测试（Actions → Run workflow）

### Q2: Docker 容器无法启动？

```bash
# 检查配置文件
docker exec -it trend-radar ls -la /app/config/

# 查看详细日志
docker logs --tail 100 trend-radar

# 检查容器状态
docker ps -a | grep trend-radar
```

### Q3: 收不到推送通知？

- 检查 GitHub Secrets 是否正确配置
- 检查 `config.yaml` 中 `enable_notification` 是否为 `true`
- 检查 webhook 地址是否有效
- 查看执行日志确认是否有错误

### Q4: 关键词不匹配？

- 检查 `frequency_words.txt` 格式是否正确
- 注意大小写敏感
- 使用 `+` 必须词和 `!` 过滤词优化匹配

### Q5: HTTP 服务无法启动？

```bash
# 检查端口占用
lsof -i :3333  # Mac/Linux
netstat -ano | findstr :3333  # Windows

# 检查依赖
uv sync

# 手动启动查看错误
uv run python -m mcp_server.server --transport http --port 3333
```

---

## 📚 相关资源

- **项目地址**：https://github.com/sansan0/TrendRadar
- **详细文档**：查看 `readme.md`
- **MCP 配置教程**：`README-Cherry-Studio.md`
- **MCP 使用 FAQ**：`README-MCP-FAQ.md`
- **Docker 镜像**：https://hub.docker.com/r/wantcat/trendradar

---

## 🎯 快速开始检查清单

- [ ] Fork 项目到 GitHub（或下载到本地）
- [ ] 配置至少一个通知渠道（企业微信/飞书/钉钉等）
- [ ] 编辑 `config/frequency_words.txt` 添加关键词
- [ ] 选择推送模式（daily/current/incremental）
- [ ] 手动测试一次（GitHub Actions 或 Docker）
- [ ] 确认收到推送通知
- [ ] （可选）配置 AI 分析功能

---

## 💡 提示

1. **首次配置**：建议先用简单的关键词测试，确认推送正常后再细化
2. **推送频率**：不要设置太频繁（建议不低于 30 分钟），避免被平台限制
3. **关键词优化**：从宽泛到精确，逐步调整关键词和过滤词
4. **数据积累**：AI 分析功能需要至少运行一天积累数据
5. **安全注意**：webhook 地址不要公开，使用 GitHub Secrets 存储

---

**祝你使用愉快！如有问题，请查看项目 Issues 或 README 文档。**

