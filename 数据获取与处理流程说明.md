# TrendRadar 数据获取与处理流程详解

## 📊 整体架构流程

```
GitHub Actions 定时触发
    ↓
加载配置文件 (config.yaml, frequency_words.txt)
    ↓
数据获取阶段 (DataFetcher)
    ├─ 调用 newsnow API 获取各平台数据
    ├─ 解析 JSON 响应
    └─ 保存原始数据到文件
    ↓
数据处理阶段
    ├─ 读取历史数据
    ├─ 检测新增新闻
    └─ 合并重复标题
    ↓
关键词匹配阶段
    ├─ 加载频率词配置
    ├─ 匹配普通词/必须词/过滤词
    └─ 按词组分组统计
    ↓
权重计算与排序
    ├─ 计算新闻权重（排名+频次+热度）
    └─ 按权重排序
    ↓
报告生成
    ├─ 生成 HTML 报告
    ├─ 生成推送消息
    └─ 保存到 output 目录
    ↓
多渠道推送
    ├─ 企业微信/飞书/钉钉
    ├─ Telegram/邮件/ntfy
    └─ GitHub Pages（网页版）
```

---

## 🔍 详细流程解析

### 1. 数据获取阶段 (DataFetcher)

#### 1.1 API 调用

**核心代码位置**：`main.py` 第 434-552 行

```python
class DataFetcher:
    def fetch_data(self, id_info, max_retries=2):
        """获取指定平台的数据"""
        # 构建 API URL
        url = f"https://newsnow.busiyi.world/api/s?id={id_value}&latest"
        
        # 发送 HTTP 请求
        response = requests.get(url, headers=headers, proxies=proxies)
```

**关键点**：
- **数据来源**：使用第三方 API `newsnow.busiyi.world`，而非直接爬取各平台
- **API 格式**：`https://newsnow.busiyi.world/api/s?id={平台ID}&latest`
- **平台 ID**：来自 `config.yaml` 中的 `platforms` 配置（如 `zhihu`、`weibo`、`douyin`）

**示例请求**：
```
GET https://newsnow.busiyi.world/api/s?id=zhihu&latest
```

**返回数据格式**：
```json
{
  "items": [
    {
      "title": "新闻标题",
      "url": "https://...",
      "mobileUrl": "https://..."
    },
    ...
  ]
}
```

#### 1.2 数据解析与存储

```python
def crawl_websites(self, ids_list, request_interval=1000):
    """爬取多个网站数据"""
    results = {}
    for id_info in ids_list:
        response, _, _ = self.fetch_data(id_info)
        if response:
            data = json.loads(response)
            # 解析每个新闻项
            for index, item in enumerate(data.get("items", []), 1):
                title = item["title"]
                url = item.get("url", "")
                mobile_url = item.get("mobileUrl", "")
                
                # 存储到 results 字典
                results[id_value][title] = {
                    "ranks": [index],  # 排名
                    "url": url,
                    "mobileUrl": mobile_url,
                }
```

**数据结构**：
```python
results = {
    "zhihu": {
        "ChatGPT-5正式发布": {
            "ranks": [1],  # 排名第1
            "url": "https://...",
            "mobileUrl": "https://..."
        },
        "AI芯片概念股暴涨": {
            "ranks": [3],
            "url": "https://...",
            "mobileUrl": "https://..."
        }
    },
    "weibo": {
        ...
    }
}
```

#### 1.3 保存到文件

**代码位置**：`main.py` 第 556-603 行

```python
def save_titles_to_file(results, id_to_name, failed_ids):
    """保存标题到文件"""
    file_path = get_output_path("txt", f"{format_time_filename()}.txt")
    
    with open(file_path, "w", encoding="utf-8") as f:
        for id_value, title_data in results.items():
            # 写入平台信息
            f.write(f"{id_value} | {name}\n")
            
            # 按排名排序并写入
            for rank, title, url, mobile_url in sorted_titles:
                line = f"{rank}. {title}"
                if url:
                    line += f" [URL:{url}]"
                if mobile_url:
                    line += f" [MOBILE:{mobile_url}]"
                f.write(line + "\n")
```

**文件格式示例**：
```
zhihu | 知乎
1. ChatGPT-5正式发布 [URL:https://...] [MOBILE:https://...]
3. AI芯片概念股暴涨 [URL:https://...] [MOBILE:https://...]

weibo | 微博
1. 比亚迪月销量破纪录 [URL:https://...] [MOBILE:https://...]
```

**文件保存位置**：
```
output/
  └── 2025-01-15/
      └── txt/
          └── 2025-01-15_12-30-15.txt
```

---

### 2. 数据处理阶段

#### 2.1 读取历史数据

**代码位置**：`main.py` 第 730-771 行

```python
def read_all_today_titles(current_platform_ids=None):
    """读取当天所有标题文件"""
    date_folder = format_date_folder()  # 如 "2025-01-15"
    txt_dir = Path("output") / date_folder / "txt"
    
    all_results = {}  # 合并所有文件的数据
    title_info = {}   # 记录每条新闻的时间信息
    
    # 遍历当天所有 txt 文件
    for file_path in sorted(files):
        titles_by_id, file_id_to_name = parse_file_titles(file_path)
        
        # 合并数据，处理重复标题
        for source_id, title_data in titles_by_id.items():
            process_source_data(source_id, title_data, time_info, 
                              all_results, title_info)
```

**关键处理逻辑**：

1. **合并重复标题**：如果同一条新闻在多个时间点出现，会合并排名信息
2. **记录时间信息**：
   - `first_time`：首次出现时间
   - `last_time`：最后出现时间
   - `count`：出现次数
   - `ranks`：所有出现过的排名列表

#### 2.2 检测新增新闻

**代码位置**：`main.py` 第 847-903 行

```python
def detect_latest_new_titles(current_platform_ids=None):
    """检测当日最新批次的新增标题"""
    files = sorted([f for f in txt_dir.iterdir() if f.suffix == ".txt"])
    
    # 解析最新文件
    latest_file = files[-1]
    latest_titles, _ = parse_file_titles(latest_file)
    
    # 汇总历史标题
    historical_titles = {}
    for file_path in files[:-1]:
        historical_data, _ = parse_file_titles(file_path)
        for source_id, titles_data in historical_data.items():
            historical_titles[source_id].add(title)
    
    # 找出新增标题（在最新文件中但不在历史文件中）
    new_titles = {}
    for source_id, latest_source_titles in latest_titles.items():
        historical_set = historical_titles.get(source_id, set())
        for title, title_data in latest_source_titles.items():
            if title not in historical_set:
                new_titles[source_id][title] = title_data
```

**用途**：
- 在 `current` 和 `daily` 模式下，标记新增新闻（显示 🆕）
- 在 `incremental` 模式下，只推送新增新闻

---

### 3. 关键词匹配阶段

#### 3.1 加载频率词配置

**代码位置**：`main.py` 第 606-657 行

```python
def load_frequency_words(frequency_file=None):
    """加载频率词配置"""
    with open(frequency_file, "r", encoding="utf-8") as f:
        content = f.read()
    
    # 按空行分割词组
    word_groups = [group.strip() for group in content.split("\n\n") 
                   if group.strip()]
    
    processed_groups = []
    filter_words = []
    
    for group in word_groups:
        words = [word.strip() for word in group.split("\n") if word.strip()]
        
        for word in words:
            if word.startswith("!"):
                # 过滤词
                filter_words.append(word[1:])
            elif word.startswith("+"):
                # 必须词
                group_required_words.append(word[1:])
            else:
                # 普通词
                group_normal_words.append(word)
        
        processed_groups.append({
            "required": group_required_words,  # 必须词列表
            "normal": group_normal_words,      # 普通词列表
            "group_key": group_key             # 词组标识
        })
    
    return processed_groups, filter_words
```

**配置示例** (`frequency_words.txt`)：
```txt
华为
OPPO
+发布

A股
上证
+涨跌
!预测
```

**解析结果**：
```python
word_groups = [
    {
        "required": ["发布"],
        "normal": ["华为", "OPPO"],
        "group_key": "华为 OPPO"
    },
    {
        "required": ["涨跌"],
        "normal": ["A股", "上证"],
        "group_key": "A股 上证"
    }
]
filter_words = ["预测"]
```

#### 3.2 标题匹配逻辑

**代码位置**：`main.py` 第 943-980 行

```python
def matches_word_groups(title, word_groups, filter_words):
    """检查标题是否匹配词组规则"""
    title_lower = title.lower()
    
    # 1. 先检查过滤词（优先级最高）
    if any(filter_word.lower() in title_lower for filter_word in filter_words):
        return False  # 包含过滤词，直接排除
    
    # 2. 检查词组匹配
    for group in word_groups:
        required_words = group["required"]  # 必须词
        normal_words = group["normal"]       # 普通词
        
        # 2.1 检查必须词（如果配置了必须词，必须全部包含）
        if required_words:
            all_required_present = all(
                req_word.lower() in title_lower 
                for req_word in required_words
            )
            if not all_required_present:
                continue  # 必须词不满足，跳过这个词组
        
        # 2.2 检查普通词（至少包含一个）
        if normal_words:
            any_normal_present = any(
                normal_word.lower() in title_lower 
                for normal_word in normal_words
            )
            if not any_normal_present:
                continue  # 普通词不满足，跳过这个词组
        
        # 如果通过所有检查，匹配成功
        return True
    
    return False  # 所有词组都不匹配
```

**匹配示例**：

| 标题 | 配置 | 匹配结果 | 说明 |
|------|------|---------|------|
| "iPhone 15正式发布售价公布" | `iPhone`<br/>`+发布` | ✅ 匹配 | 包含"iPhone"和"发布" |
| "iPhone销量创新高" | `iPhone`<br/>`+发布` | ❌ 不匹配 | 缺少必须词"发布" |
| "专家预测A股涨跌趋势" | `A股`<br/>`+涨跌`<br/>`!预测` | ❌ 不匹配 | 包含过滤词"预测" |
| "A股今日大幅涨跌分析" | `A股`<br/>`+涨跌`<br/>`!预测` | ✅ 匹配 | 包含"A股"和"涨跌"，且不包含"预测" |

#### 3.3 词频统计

**代码位置**：`main.py` 第 1033-1340 行

```python
def count_word_frequency(results, word_groups, filter_words, 
                        id_to_name, title_info, mode="daily"):
    """统计词频，支持必须词、频率词、过滤词"""
    
    stats = []  # 存储每个词组的统计结果
    
    # 遍历每个词组
    for group in word_groups:
        group_key = group["group_key"]
        matched_news = []
        
        # 遍历所有平台的新闻
        for source_id, title_data in results.items():
            platform_name = id_to_name.get(source_id, source_id)
            
            for title, data in title_data.items():
                # 检查是否匹配当前词组
                if matches_word_groups(title, [group], filter_words):
                    # 计算权重
                    weight = calculate_news_weight(
                        title_info.get(source_id, {}).get(title, {}),
                        rank_threshold
                    )
                    
                    matched_news.append({
                        "title": title,
                        "platform": platform_name,
                        "platform_id": source_id,
                        "weight": weight,
                        "ranks": data.get("ranks", []),
                        "url": data.get("url", ""),
                        "mobileUrl": data.get("mobileUrl", ""),
                        "is_new": is_new_title(title, source_id, new_titles)
                    })
        
        # 按权重排序
        matched_news.sort(key=lambda x: x["weight"], reverse=True)
        
        stats.append({
            "group_key": group_key,
            "news": matched_news,
            "count": len(matched_news)
        })
    
    # 按匹配新闻数量排序
    stats.sort(key=lambda x: x["count"], reverse=True)
    
    return stats, total_titles
```

---

### 4. 权重计算与排序

#### 4.1 权重计算公式

**代码位置**：`main.py` 第 907-940 行

```python
def calculate_news_weight(title_data, rank_threshold=5):
    """计算新闻权重，用于排序"""
    ranks = title_data.get("ranks", [])
    count = title_data.get("count", len(ranks))
    weight_config = CONFIG["WEIGHT_CONFIG"]
    
    # 1. 排名权重：Σ(11 - min(rank, 10)) / 出现次数
    rank_scores = []
    for rank in ranks:
        score = 11 - min(rank, 10)  # 排名1得10分，排名10得1分
        rank_scores.append(score)
    rank_weight = sum(rank_scores) / len(ranks) if ranks else 0
    
    # 2. 频次权重：min(出现次数, 10) × 10
    frequency_weight = min(count, 10) * 10  # 最多100分
    
    # 3. 热度加成：高排名次数 / 总出现次数 × 100
    high_rank_count = sum(1 for rank in ranks if rank <= rank_threshold)
    hotness_ratio = high_rank_count / len(ranks) if ranks else 0
    hotness_weight = hotness_ratio * 100  # 0-100分
    
    # 4. 综合权重（默认配置：排名60% + 频次30% + 热度10%）
    total_weight = (
        rank_weight * weight_config["RANK_WEIGHT"] +      # 0.6
        frequency_weight * weight_config["FREQUENCY_WEIGHT"] +  # 0.3
        hotness_weight * weight_config["HOTNESS_WEIGHT"]  # 0.1
    )
    
    return total_weight
```

**权重计算示例**：

假设一条新闻：
- 排名：`[1, 3, 5]`（出现过3次，分别排第1、3、5名）
- 出现次数：`3`
- 高排名次数：`1`（只有1次排名≤5）

计算过程：
1. **排名权重**：
   - 排名1：`11 - min(1, 10) = 10` 分
   - 排名3：`11 - min(3, 10) = 8` 分
   - 排名5：`11 - min(5, 10) = 6` 分
   - 平均：`(10 + 8 + 6) / 3 = 8` 分
   - 加权：`8 × 0.6 = 4.8`

2. **频次权重**：
   - `min(3, 10) × 10 = 30` 分
   - 加权：`30 × 0.3 = 9`

3. **热度权重**：
   - `1 / 3 × 100 = 33.33` 分
   - 加权：`33.33 × 0.1 = 3.33`

4. **总权重**：`4.8 + 9 + 3.33 = 17.13`

---

### 5. 报告生成阶段

#### 5.1 准备报告数据

**代码位置**：`main.py` 第 1343-1370 行

```python
def prepare_report_data(stats, failed_ids, new_titles, 
                       id_to_name, mode="daily"):
    """准备报告数据"""
    
    # 处理新增新闻区域
    processed_new_titles = []
    if new_titles and id_to_name:
        for source_id, titles_data in new_titles.items():
            platform_name = id_to_name.get(source_id, source_id)
            for title, title_data in titles_data.items():
                # 只包含匹配关键词的新增新闻
                if matches_word_groups(title, word_groups, filter_words):
                    processed_new_titles.append({
                        "platform": platform_name,
                        "title": title,
                        "rank": title_data.get("ranks", [1])[0],
                        "url": title_data.get("url", ""),
                        "mobileUrl": title_data.get("mobileUrl", "")
                    })
    
    report_data = {
        "stats": stats,                    # 词组统计
        "new_titles": processed_new_titles, # 新增新闻
        "failed_ids": failed_ids,           # 失败的平台
        "total_titles": total_titles,       # 总新闻数
        "timestamp": datetime.now().strftime("%Y-%m-%d %H:%M:%S")
    }
    
    return report_data
```

#### 5.2 生成 HTML 报告

**代码位置**：`main.py` 第 1500-2000+ 行（HTML 生成函数）

主要步骤：
1. 生成 HTML 头部和样式
2. 遍历词组统计，生成新闻列表
3. 生成新增新闻区域
4. 保存到 `output/日期/html/时间.html`

#### 5.3 生成推送消息

**代码位置**：`main.py` 第 2000-3000+ 行（各平台推送函数）

推送消息格式示例：
```
🔥 [1/3] AI ChatGPT : 2 条

  1. [百度热搜] 🆕 ChatGPT-5正式发布 [**1**] - 09时15分 (1次)
  
  2. [今日头条] AI芯片概念股暴涨 [**3**] - [08时30分 ~ 10时45分] (3次)

━━━━━━━━━━━━━━━━━━━

🆕 本次新增热点新闻 (共 2 条)

**百度热搜** (1 条):
  1. ChatGPT-5正式发布 [**1**]

更新时间：2025-01-15 12:30:15
```

---

### 6. 三种推送模式的区别

#### 6.1 daily（当日汇总模式）

**代码位置**：`main.py` 第 1063-1070 行

```python
if mode == "daily":
    # 处理当天所有累计数据
    results_to_process = results  # 所有历史数据
    all_news_are_new = False
```

**特点**：
- 处理当天所有累计的新闻数据
- 显示每条新闻的完整时间跨度（首次出现 ~ 最后出现）
- 显示出现次数

#### 6.2 current（当前榜单模式）

**代码位置**：`main.py` 第 1071-1080 行

```python
elif mode == "current":
    # 只处理最新一批数据
    results_to_process = results  # 使用最新抓取的数据
    all_news_are_new = False
    # 但会标记新增新闻（🆕）
```

**特点**：
- 只处理最新一次抓取的数据
- 显示当前榜单的匹配新闻
- 标记新增新闻（🆕）

#### 6.3 incremental（增量监控模式）

**代码位置**：`main.py` 第 1054-1062 行

```python
if mode == "incremental":
    if is_first_today:
        # 当天第一次：处理所有新闻，都标记为新增
        results_to_process = results
        all_news_are_new = True
    else:
        # 当天非第一次：只处理新增的新闻
        results_to_process = new_titles if new_titles else {}
        all_news_are_new = True
```

**特点**：
- 只推送新增的新闻
- 避免重复信息干扰
- 适合高频监控场景

---

## 🔄 完整执行流程示例

假设当前时间是 `2025-01-15 14:00`，执行 `current` 模式：

1. **GitHub Actions 触发** → 执行 `main.py`

2. **加载配置**：
   - 读取 `config/config.yaml` → 模式：`current`
   - 读取 `config/frequency_words.txt` → 关键词：`AI`, `ChatGPT`

3. **数据获取**：
   - 调用 `https://newsnow.busiyi.world/api/s?id=zhihu&latest`
   - 调用 `https://newsnow.busiyi.world/api/s?id=weibo&latest`
   - ...（11个平台）
   - 保存到 `output/2025-01-15/txt/2025-01-15_14-00-00.txt`

4. **数据处理**：
   - 读取当天所有历史文件
   - 检测新增新闻（对比最新文件和历史文件）
   - 合并重复标题

5. **关键词匹配**：
   - 遍历所有新闻标题
   - 检查是否包含 `AI` 或 `ChatGPT`
   - 过滤掉包含过滤词的标题

6. **权重计算**：
   - 对匹配的新闻计算权重
   - 按权重排序

7. **报告生成**：
   - 生成 HTML 报告
   - 生成推送消息文本

8. **推送通知**：
   - 发送到企业微信/飞书/钉钉等
   - 提交 HTML 到 GitHub Pages

---

## 📝 关键数据结构

### 新闻数据结构

```python
{
    "title": "ChatGPT-5正式发布",
    "platform": "百度热搜",
    "platform_id": "baidu",
    "ranks": [1, 2, 3],           # 出现过的所有排名
    "url": "https://...",
    "mobileUrl": "https://...",
    "first_time": "09时15分",     # 首次出现时间
    "last_time": "10时45分",      # 最后出现时间
    "count": 3,                   # 出现次数
    "weight": 17.13,              # 计算出的权重
    "is_new": True                # 是否新增
}
```

### 词组统计结构

```python
{
    "group_key": "AI ChatGPT",
    "news": [
        {
            "title": "ChatGPT-5正式发布",
            "platform": "百度热搜",
            "weight": 17.13,
            ...
        },
        ...
    ],
    "count": 2  # 匹配的新闻数量
}
```

---

## 🎯 总结

**核心流程**：
1. **API 获取** → 使用 `newsnow.busiyi.world` API 获取各平台数据
2. **数据存储** → 保存到 `output/日期/txt/` 目录
3. **关键词匹配** → 使用 `frequency_words.txt` 配置进行筛选
4. **权重排序** → 根据排名、频次、热度计算权重并排序
5. **报告生成** → 生成 HTML 和推送消息
6. **多渠道推送** → 发送到配置的通知渠道

**关键优势**：
- ✅ 不直接爬取，使用统一 API，稳定可靠
- ✅ 支持历史数据追踪，记录新闻生命周期
- ✅ 灵活的关键词匹配（普通词/必须词/过滤词）
- ✅ 智能权重算法，突出重要新闻
- ✅ 三种推送模式，适应不同场景

